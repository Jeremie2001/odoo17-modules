<odoo>
    <!-- Formulaire -->
    <record model="ir.ui.view" id="sale_order_form_inherit_project">
        <field name="name">sale.order.form.inherit.project</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="projet"/>
            </xpath>
        </field>
    </record>

    <!-- Arbre -->
    <record model="ir.ui.view" id="sale_order_tree_inherit_project">
        <field name="name">sale.order.tree.inherit.project</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="projet" readonly="1"/>
            </xpath>
        </field>
    </record>

    <!-- Filtre -->
    <record model="ir.ui.view" id="sale_order_filter_inherit_project">
        <field name="name">sale.order.filter.inherit.project</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="projet" string="Projet"/>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="sale_order_tree_inherit_project_quotation">
    <field name="name">sale.order.tree.inherit.project.quotation</field>
    <field name="model">sale.order</field>
    <field name="inherit_id" ref="sale.view_quotation_tree"/>
    <field name="arch" type="xml">
        <xpath expr="//field[@name='partner_id']" position="after">
            <field name="projet" readonly="1"/>
        </xpath>
    </field>
</record>


  

</odoo>
