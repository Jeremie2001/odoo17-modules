<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale_copy_wizard_view_form" model="ir.ui.view">
        <field name="name">sale.copy.wizard.form</field>
        <field name="model">sale.copy.wizard</field>
        <field name="arch" type="xml">
            <form string="Copy Sale Order to Another Company">
                <group>
                    <field name="current_company_id" invisible="1"/>
                    <field name="sale_order_ids" readonly="1" widget="many2many_tags"/>
                    <field name="target_company_id" required="1"/>
                    <field name="copy_notes"/>
                </group>
                <footer>
                    <button
                        name="action_copy_to_company"
                        string="Copy Order"
                        type="object"
                        class="btn-primary"
                    />
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_sale_copy_wizard" model="ir.actions.act_window">
        <field name="name">Copy to Another Company</field>
        <field name="res_model">sale.copy.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'active_id': active_id}</field>
    </record>

</odoo>